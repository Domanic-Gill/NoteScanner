<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context="com.notescanner.NoteActivity">

    <item
        android:id="@+id/action_delete"
        android:icon="@drawable/ic_note_delete"
        android:title="Save note"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/action_save"
        android:icon="@drawable/ic_note_save"
        android:title="Save note"
        app:showAsAction="ifRoom"/> <!-- change to always if issues -->
    <item
        android:id="@+id/overflow_pdf"
        android:title="Save to pdf"
        app:showAsAction="never"/>
    <item
        android:id="@+id/overflow_docx"
        android:title="Save to docx"
        app:showAsAction="never" />
</menu>